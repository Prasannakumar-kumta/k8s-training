apiVersion: v1
kind: pod
metadata:
  name: pod1
  labels:
    app: helloworld
spec:
  containers:
  - name: k8s-demo
    image: Prasannakumar-kumta/k8s-demo
    ports:
    - name: nodejs-port
      containerPort: 3000
